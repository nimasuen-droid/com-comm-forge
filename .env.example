# Local-only developer defaults.
# Cloudflare runtime variables for staging/production live in wrangler.jsonc.

APP_ENV=local
APP_NAME="Completions & Commissioning Pro"
PUBLIC_APP_URL=http://localhost:5173
ERROR_MONITORING_ENABLED=false
VITE_ENABLE_PWA=false

# Add provider DSNs/tokens only through local .env files or hosting secrets.
# Do not commit real secrets.
SENTRY_DSN=
POSTHOG_KEY=
